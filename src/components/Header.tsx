//server component by default
export default function Header(){
    return (
        <header className="text-center min-h-[50px] flex items-center justify-center bg-slate-200">
            <h1>Header</h1>
        </header>
    )
} 